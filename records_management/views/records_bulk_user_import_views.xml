<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.bulk.user.import -->
    <record id="view_records_bulk_user_import_tree" model="ir.ui.view">
        <field name="name">records.bulk.user.import.tree</field>
        <field name="model">records.bulk.user.import</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.bulk.user.import -->
    <record id="view_records_bulk_user_import_form" model="ir.ui.view">
        <field name="name">records.bulk.user.import.form</field>
        <field name="model">records.bulk.user.import</field>
        <field name="arch" type="xml">
            <form string="Records Bulk User Import">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.bulk.user.import -->
    <record id="view_records_bulk_user_import_search" model="ir.ui.view">
        <field name="name">records.bulk.user.import.search</field>
        <field name="model">records.bulk.user.import</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.bulk.user.import -->
    <record id="action_records_bulk_user_import" model="ir.actions.act_window">
        <field name="name">Records Bulk User Import</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.bulk.user.import</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Bulk User Import.
            </p>
        </field>
    </record>

</odoo>
