<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View bin.key.history View List -->
    <record id="view_bin_key_history_list" model="ir.ui.view">
        <field name="name">view.bin.key.history.list</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View bin.key.history form -->
    <record id="view_bin_key_history_form" model="ir.ui.view">
        <field name="name">view.bin.key.history.form</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
            <form string="Bin Key History">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View bin.key.history search -->
    <record id="view_bin_key_history_search" model="ir.ui.view">
        <field name="name">view.bin.key.history.search</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action bin.key.history -->
    <record id="action_bin_key_history" model="ir.actions.act_window">
        <field name="name">Bin Key History</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">bin.key.history</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Bin Key History.
            </p>
        </field>
    </record>

</odoo>
