<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
<!-- List view for bin.key.history -->
    <record id="bin_key_history_view_tree" model="ir.ui.view">
        <field name="name">bin.key.history.view.tree</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
        <list string="Bin Key History" decoration-info="event_type == 'assign'" decoration-success="event_type == 'return'" decoration-warning="event_type == 'lost'" decoration-danger="event_type == 'retire'">
        <field name="key_id"/>
        <field name="user_id"/>
        <field name="event_type" widget="badge"/>
        <field name="event_date"/>
        <field name="company_id" groups="base.group_multi_company"/>

        </list>
    </field>
    </record>

<!-- Form view for bin.key.history -->
    <record id="bin_key_history_view_form" model="ir.ui.view">
        <field name="name">bin.key.history.view.form</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
        <form string="Bin Key History Record" create="false" edit="false">
            <sheet>
                <div class="oe_title">
                    <h1>
                        <field name="name" readonly="1"/>
                    </h1>
                </div>
                <group>
                    <group string="Event Details">
                        <field name="key_id" readonly="1"/>
                        <field name="user_id" readonly="1"/>
                        <field name="event_type" widget="badge" readonly="1"/>
                        <field name="event_date" readonly="1"/>
                    </group>
                    <group string="Assignment Info">
                        <field name="partner_id" readonly="1"/>
                        <field name="company_id" readonly="1"/>
                    </group>
                </group>
                <group>
                    <group string="Additional Information">
                        <field name="notes" widget="text" readonly="1"/>
                    </group>
                </group>
            </sheet>
            <div class="oe_chatter">
                <field name="message_follower_ids" widget="mail_followers"/>
                <field name="activity_ids" widget="mail_activity"/>
                <field name="message_ids" widget="mail_thread"/>
            </div>
        </form>
    </field>
    </record>

<!-- Kanban view for bin.key.history -->
    <record id="bin_key_history_view_kanban" model="ir.ui.view">
        <field name="name">bin.key.history.view.kanban</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
    <kanban class="o_kanban_mobile">
        <field name="key_id"/>
        <field name="user_id"/>
        <field name="event_type"/>
        <field name="event_date"/>
        <templates>
            <t t-name="card">
                <div class="oe_kanban_card oe_kanban_global_click">
                    <div class="o_kanban_record_top mb-2">
                        <div class="o_kanban_record_headings">
                            <strong class="o_kanban_record_title">
                                <field name="key_id"/>
                            </strong>
                        </div>
                        <div class="o_kanban_record_subtitle text-muted">
                            <field name="user_id"/>
                        </div>
                    </div>
                    <div class="o_kanban_record_body">
                        <span class="badge" t-attf-class="badge-#{event_type == 'assign' and 'info' or event_type == 'return' and 'success' or event_type == 'lost' and 'warning' or 'danger'}">
                            <field name="event_type"/>
                        </span>
                        <br/>
                        <i class="fa fa-calendar" aria-hidden="true"/>
                        <field name="event_date"/>
                    </div>
                </div>
            </t>
        </templates>
    </kanban>
</field>
    </record>

<!-- Search view for bin.key.history -->
    <record id="bin_key_history_view_search" model="ir.ui.view">
        <field name="name">bin.key.history.view.search</field>
        <field name="model">bin.key.history</field>
        <field name="arch" type="xml">
        <search string="Search Key History">
        <field name="name" string="Record Name"/>
        <field name="key_id" string="Key"/>
        <field name="user_id" string="User"/>
        <field name="partner_id" string="Customer"/>
        <field name="event_type" string="Event Type"/>

        <separator/>

        <filter string="Created" name="created" domain="[('event_type', '=', 'create')]"/>
        <filter string="Assigned" name="assigned" domain="[('event_type', '=', 'assign')]"/>
        <filter string="Returned" name="returned" domain="[('event_type', '=', 'return')]"/>
        <filter string="Lost" name="lost" domain="[('event_type', '=', 'lost')]"/>

        <separator/>

        <filter string="This Month" name="this_month" domain="[('event_date', '&gt;=', (context_today - datetime.timedelta(days=30)).strftime('%Y-%m-%d'))]"/>

        <group expand="1" string="Group By">
            <filter string="Event Type" name="group_event_type" domain="[]" context="{'group_by': 'event_type'}"/>
            <filter string="Key" name="group_key" domain="[]" context="{'group_by': 'key_id'}"/>
            <filter string="User" name="group_user" domain="[]" context="{'group_by': 'user_id'}"/>
            <filter string="Event Date" name="group_event_date" domain="[]" context="{'group_by': 'event_date:month'}"/>
            <filter string="Company" name="group_company" domain="[]" context="{'group_by': 'company_id'}" groups="base.group_multi_company"/>
        </group>
        </search>
    </field>
    </record>

<!-- Action for bin.key.history -->
    <record id="action_bin_key_history" model="ir.actions.act_window">
        <field name="name">Bin Key History</field>
        <field name="res_model">bin.key.history</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[]</field>
        <field name="context">{'search_default_this_month': 1}</field>
        <field name="help" type="html">
        <p class="oe_view_nocontent_create">
No key history records found.
</p>
<p>
Key history records are automatically created when keys are assigned,
            returned, or when other actions are performed. This provides a complete
            audit trail for compliance and security monitoring.
</p>
<p>
<strong>Key Features:</strong>
<br/>
            • Complete assignment and return tracking<br/>
            • Emergency access logging<br/>
            • Security deposit management<br/>
            • Authorization audit trails<br/>
            • NAID AAA compliance support
        </p>
    </field>
    </record>
    </data>
</odoo>
