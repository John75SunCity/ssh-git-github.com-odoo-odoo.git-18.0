<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.retrieval.work.order -->
    <record id="records_retrieval_work_order_view_tree" model="ir.ui.view">
        <field name="name">records.retrieval.work.order.view.tree</field>
        <field name="model">records.retrieval.work.order</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.retrieval.work.order -->
    <record id="records_retrieval_work_order_view_form" model="ir.ui.view">
        <field name="name">records.retrieval.work.order.view.form</field>
        <field name="model">records.retrieval.work.order</field>
        <field name="arch" type="xml">
            <form string="Records.Retrieval.Work.Order">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.retrieval.work.order -->
    <record id="records_retrieval_work_order_view_search" model="ir.ui.view">
        <field name="name">records.retrieval.work.order.view.search</field>
        <field name="model">records.retrieval.work.order</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action for records.retrieval.work.order -->
    <record id="action_records_retrieval_work_order" model="ir.actions.act_window">
        <field name="name">Records.Retrieval.Work.Order</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.retrieval.work.order</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to add a new records.retrieval.work.order.
            </p>
        </field>
    </record>

</odoo>
