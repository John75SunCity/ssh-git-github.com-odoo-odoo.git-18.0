<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Tree View -->
        <record id="records_container_content_view_tree" model="ir.ui.view">
            <field name="name">records.container.content.view.tree</field>
            <field name="model">records.container.content</field>
            <field name="arch" type="xml">
                <tree string="Records Container Content">
                    <!-- Add fields here -->
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="records_container_content_view_form" model="ir.ui.view">
            <field name="name">records.container.content.view.form</field>
            <field name="model">records.container.content</field>
            <field name="arch" type="xml">
                <form string="Records Container Content">
                    <sheet>
                        <group>
                            <!-- Add fields here -->
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Search View -->
        <record id="records_container_content_view_search" model="ir.ui.view">
            <field name="name">records.container.content.view.search</field>
            <field name="model">records.container.content</field>
            <field name="arch" type="xml">
                <search string="Search Records Container Content">
                    <!-- Add search fields here -->
                </search>
            </field>
        </record>

        <!-- Action -->
        <record id="action_records_container_content" model="ir.actions.act_window">
            <field name="name">Records Container Content</field>
            <field name="res_model">records.container.content</field>
            <field name="view_mode">tree,form</field>
        </record>

    </data>
</odoo>
