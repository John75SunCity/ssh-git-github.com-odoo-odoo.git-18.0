<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.department.billing.contact -->
    <record id="view_records_department_billing_contact_tree" model="ir.ui.view">
        <field name="name">records.department.billing.contact.tree</field>
        <field name="model">records.department.billing.contact</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.department.billing.contact -->
    <record id="view_records_department_billing_contact_form" model="ir.ui.view">
        <field name="name">records.department.billing.contact.form</field>
        <field name="model">records.department.billing.contact</field>
        <field name="arch" type="xml">
            <form string="Records Department Billing Contact">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.department.billing.contact -->
    <record id="view_records_department_billing_contact_search" model="ir.ui.view">
        <field name="name">records.department.billing.contact.search</field>
        <field name="model">records.department.billing.contact</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.department.billing.contact -->
    <record id="action_records_department_billing_contact" model="ir.actions.act_window">
        <field name="name">Records Department Billing Contact</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.department.billing.contact</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Department Billing Contact.
            </p>
        </field>
    </record>

</odoo>
