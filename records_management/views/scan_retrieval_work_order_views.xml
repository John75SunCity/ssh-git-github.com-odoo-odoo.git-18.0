<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for scan.retrieval.work.order -->
    <record id="view_scan_retrieval_work_order_tree" model="ir.ui.view">
        <field name="name">scan.retrieval.work.order.tree</field>
        <field name="model">scan.retrieval.work.order</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for scan.retrieval.work.order -->
    <record id="view_scan_retrieval_work_order_form" model="ir.ui.view">
        <field name="name">scan.retrieval.work.order.form</field>
        <field name="model">scan.retrieval.work.order</field>
        <field name="arch" type="xml">
            <form string="Scan Retrieval Work Order">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for scan.retrieval.work.order -->
    <record id="view_scan_retrieval_work_order_search" model="ir.ui.view">
        <field name="name">scan.retrieval.work.order.search</field>
        <field name="model">scan.retrieval.work.order</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action scan.retrieval.work.order -->
    <record id="action_scan_retrieval_work_order" model="ir.actions.act_window">
        <field name="name">Scan Retrieval Work Order</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">scan.retrieval.work.order</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Scan Retrieval Work Order.
            </p>
        </field>
    </record>

</odoo>
