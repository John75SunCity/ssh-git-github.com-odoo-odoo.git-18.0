<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree view for tags (Odoo 18.0 minimal approach) -->
    <record id="view_records_tag_tree_new" model="ir.ui.view">
        <field name="name">records.tag.tree.new</field>
        <field name="model">records.tag</field>
        <field name="arch" type="xml">
            <tree editable="bottom" sample="1">
                <field name="name"/>
                <field name="color" widget="color"/>
                <field name="description" optional="show"/>
            </tree>
        </field>
    </record>

    <!-- Form view for tags (Odoo 18.0 minimal approach) -->
    <record id="view_records_tag_form_new" model="ir.ui.view">
        <field name="name">records.tag.form.new</field>
        <field name="model">records.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Tag Name..."/>
                        </h1>
                    </div>
                    <group>
                        <field name="color" widget="color"/>
                        <field name="active"/>
                    </group>
                    <group>
                        <field name="description" nolabel="1" 
                               placeholder="Describe what this tag represents..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
    <!-- Window action for tags -->
    <record id="action_records_tag_new" model="ir.actions.act_window">
        <field name="name">Records Tags</field>
        <field name="res_model">records.tag</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first tag
            </p>
            <p>
                Tags help you categorize and organize your records efficiently.
                Use colors to make them visually distinct.
            </p>
        </field>
    </record>

    <!-- Search view for tags (Odoo 18.0 minimal approach) -->
    <record id="view_records_tag_search_new" model="ir.ui.view">
        <field name="name">records.tag.search.new</field>
        <field name="model">records.tag</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="description"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>
</odoo>
