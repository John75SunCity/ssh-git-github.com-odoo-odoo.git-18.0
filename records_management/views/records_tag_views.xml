<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree/List view for tags -->
    <record id="records_tag_view_tree" model="ir.ui.view">
        <field name="name">records.tag.tree</field>
        <field name="model">records.tag</field>
        <field name="arch" type="xml">
            <tree string="Tags" editable="bottom">
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </tree>
        </field>
    </record>

    <!-- Form view for tags -->
    <record id="records_tag_view_form" model="ir.ui.view">
        <field name="name">records.tag.form</field>
        <field name="model">records.tag</field>
        <field name="arch" type="xml">
            <form string="Tag">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
    <!-- Action to open tags -->
    <record id="action_records_tag" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">records.tag</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a tag
            </p>
            <p>
                Tags can be used to categorize and organize records.
            </p>
        </field>
    </record>
</odoo>
