<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="bin_key_view_tree" model="ir.ui.view">
            <field name="name">bin.key.view.tree</field>
            <field name="model">bin.key</field>
            <field name="arch" type="xml">
                <list string="Bin Keys" decoration-success="state == 'available'" decoration-warning="state == 'assigned'" decoration-danger="state == 'lost'" decoration-muted="state == 'retired'">
                    <field name="name" />
                    <field name="key_type" />
                    <field name="state" widget="badge" decoration-success="state == 'available'" decoration-warning="state == 'assigned'" decoration-danger="state == 'lost'" decoration-muted="state == 'retired'" />
                    <field name="key_holder_id" />
                    <field name="issue_date" />
                    <field name="return_date" />
                    <field name="company_id" optional="hide" />
                    <field name="active" optional="hide" />
                </list>
            </field>
        </record>
        <record id="bin_key_view_form" model="ir.ui.view">
            <field name="name">bin.key.view.form</field>
            <field name="model">bin.key</field>
            <field name="arch" type="xml">
                <form string="Bin Key">
                    <header>
                        <button name="action_assign_key" string="Assign Key" type="object" class="btn-primary" invisible="state != 'available'" />
                        <button name="action_return_key" string="Return Key" type="object" class="btn-warning" invisible="state != 'assigned'" />
                        <button name="action_mark_lost" string="Mark Lost" type="object" class="btn-danger" invisible="state != 'assigned'" />
                        <button name="action_retire_key" string="Retire Key" type="object" class="btn-secondary" invisible="state == 'retired'" />
                        <field name="state" widget="statusbar" statusbar_visible="available,assigned,lost,retired" />
                    </header>
                    <sheet>
                        <widget name="web_ribbon" title="Lost" bg_color="bg-danger" invisible="state != 'lost'" />
                        <widget name="web_ribbon" title="Retired" bg_color="bg-secondary" invisible="state != 'retired'" />
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only" />
                            <h1>
                                <field name="name" placeholder="Key Identifier (e.g., BK-001)" />
                            </h1>
                        </div>
                        <group>
                            <group string="Key Information">
                                <field name="key_type" />
                                <field name="physical_key_id" />
                            </group>
                            <group string="Current Assignment">
                                <field name="key_holder_id" options="{'no_create_edit': True}" />
                                <field name="issue_date" readonly="1" />
                                <field name="return_date" />
                                <field name="actual_return_date" readonly="1" />
                            </group>
                        </group>
                        <notebook>
                            <page string="Assignment History">
                                <field name="history_ids" readonly="1">
                                    <list>
                                        <field name="partner_id" />
                                        <field name="event_type" />
                                        <field name="event_date" />
                                        <field name="notes" />
                                    </list>
                                </field>
                            </page>
                            <page string="Notes &amp; Details">
                                <group>
                                    <field name="description" widget="text" placeholder="Key description and purpose..." />
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" />
                        <field name="activity_ids" />
                        <field name="message_ids" />
                    </div>
                </form>
            </field>
        </record>
        <record id="bin_key_view_kanban" model="ir.ui.view">
            <field name="name">bin.key.view.kanban</field>
            <field name="model">bin.key</field>
            <field name="arch" type="xml">
                <kanban default_group_by="state" class="o_kanban_small_column">
                    <field name="name" />
                    <field name="key_type" />
                    <field name="state" />
                    <field name="key_holder_id" />
                    <field name="issue_date" />
                    <templates>
                        <t t-name="card">
                            <div class="oe_kanban_card oe_kanban_global_click">
                                <div class="o_kanban_image">
                                    <i class="fa fa-key fa-2x" aria-hidden="true" t-att-style="record.state.raw_value == 'available' and 'color: #28a745;' or record.state.raw_value == 'assigned' and 'color: #ffc107;' or record.state.raw_value == 'lost' and 'color: #dc3545;' or 'color: #6c757d;'" />
                                </div>
                                <div class="oe_kanban_details">
                                    <strong class="o_kanban_record_title">
                                        <field name="name" />
                                    </strong>
                                    <div class="o_kanban_record_body">
                                        <div t-if="record.key_holder_id.raw_value">
                                            <strong>Holder:</strong>
                                            <field name="key_holder_id" />
                                        </div>
                                        <div t-if="record.issue_date.raw_value">
                                            <strong>Since:</strong>
                                            <field name="issue_date" />
                                        </div>
                                    </div>
                                    <div class="oe_kanban_record_bottom">
                                        <div class="oe_kanban_bottom_left">
                                            <span class="badge" t-att-class="record.key_type.raw_value == 'master' and 'badge-primary' or record.key_type.raw_value == 'standard' and 'badge-secondary' or 'badge-info'">
                                                <field name="key_type" />
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>
        <record id="bin_key_view_search" model="ir.ui.view">
            <field name="name">bin.key.view.search</field>
            <field name="model">bin.key</field>
            <field name="arch" type="xml">
                <search string="Search Bin Keys">
                    <field name="name" string="Key Name/ID" filter_domain="[('name', 'ilike', self)]" />
                    <field name="key_holder_id" string="Current Holder" />
                    <field name="key_type" />
                    <separator />
                    <filter string="Available Keys" name="available" domain="[('state', '=', 'available')]" />
                    <filter string="Assigned Keys" name="assigned" domain="[('state', '=', 'assigned')]" />
                    <filter string="Lost Keys" name="lost" domain="[('state', '=', 'lost')]" />
                    <filter string="Retired Keys" name="retired" domain="[('state', '=', 'retired')]" />
                    <separator />
                    <filter string="Master Keys" name="master_keys" domain="[('key_type', '=', 'master')]" />
                    <filter string="Standard Keys" name="standard_keys" domain="[('key_type', '=', 'standard')]" />
                    <separator />
                    <filter string="Overdue Returns" name="overdue_returns" domain="[('return_date', '&lt;', context_today), ('state', '=', 'assigned')]" />
                    <separator />
                    <filter string="Archived" name="inactive" domain="[('active', '=', False)]" />
                    <group expand="1" string="Group By">
                        <filter string="Status" name="group_status" domain="[]" context="{'group_by': 'state'}" />
                        <filter string="Key Type" name="group_type" domain="[]" context="{'group_by': 'key_type'}" />
                        <filter string="Current Holder" name="group_holder" domain="[]" context="{'group_by': 'key_holder_id'}" />
                        <filter string="Assigned Date" name="group_assigned_date" domain="[]" context="{'group_by': 'issue_date:month'}" />
                    </group>
                </search>
            </field>
        </record>
        <record id="action_bin_key" model="ir.actions.act_window">
            <!-- OPTIMIZATION: Model bin.key has state field - consider using default search filters -->
            <field name="name">Bin Keys</field>
            <field name="res_model">bin.key</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="domain">[]</field>
            <field name="context">{'search_default_available': 1}</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
Click to create a new bin key.
</p>
                <p>
            Manage physical keys for secure bin access. Track key assignments,
            monitor returns, and maintain audit trails for compliance.
        </p>
            </field>
        </record>
    </data>
</odoo>
