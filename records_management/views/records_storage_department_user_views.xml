<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.storage.department.user -->
    <record id="view_records_storage_department_user_tree" model="ir.ui.view">
        <field name="name">records.storage.department.user.tree</field>
        <field name="model">records.storage.department.user</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.storage.department.user -->
    <record id="view_records_storage_department_user_form" model="ir.ui.view">
        <field name="name">records.storage.department.user.form</field>
        <field name="model">records.storage.department.user</field>
        <field name="arch" type="xml">
            <form string="Records Storage Department User">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.storage.department.user -->
    <record id="view_records_storage_department_user_search" model="ir.ui.view">
        <field name="name">records.storage.department.user.search</field>
        <field name="model">records.storage.department.user</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.storage.department.user -->
    <record id="action_records_storage_department_user" model="ir.actions.act_window">
        <field name="name">Records Storage Department User</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.storage.department.user</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Storage Department User.
            </p>
        </field>
    </record>

</odoo>
