<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for revenue.forecaster -->
    <record id="view_revenue_forecaster_tree" model="ir.ui.view">
        <field name="name">revenue.forecaster.tree</field>
        <field name="model">revenue.forecaster</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for revenue.forecaster -->
    <record id="view_revenue_forecaster_form" model="ir.ui.view">
        <field name="name">revenue.forecaster.form</field>
        <field name="model">revenue.forecaster</field>
        <field name="arch" type="xml">
            <form string="Revenue Forecaster">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for revenue.forecaster -->
    <record id="view_revenue_forecaster_search" model="ir.ui.view">
        <field name="name">revenue.forecaster.search</field>
        <field name="model">revenue.forecaster</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action revenue.forecaster -->
    <record id="action_revenue_forecaster" model="ir.actions.act_window">
        <field name="name">Revenue Forecaster</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">revenue.forecaster</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Revenue Forecaster.
            </p>
        </field>
    </record>

</odoo>
