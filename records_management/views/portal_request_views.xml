<?xml version="1.0" encoding="utf-8"?>
<odoo>
<!-- Enhanced Kanban view for Portal Requests -->
    <record id="portal_request_view_kanban" model="ir.ui.view">
        <field name="name">portal.request.view.kanban</field>
        <field name="model">portal.request</field>
        <field name="arch" type="xml">
<kanban default_group_by="state" class="o_kanban_portal_request">
                <field name="name"/>
                <field name="request_type"/>
                <field name="partner_id"/>
<field name="state"/>

                <field name="priority"/>
<field name="requested_date"/>
<field name="user_id"/>
<field name="active"/>

                <templates>
<t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_card_header">
<strong>📋    <field name="name"/>
</strong>
                                <span class="float-end">
                                    <field name="priority" widget="priority"/>
                                </span>
<div class="text-muted">👤    <field name="partner_id"/>
</div>
                            </div>
                            <div class="o_kanban_card_content">
                                <div class="mb-2">
<strong>Type:</strong>
<field name="request_type"/>

                                    <br/>
<strong>Requested:</strong>
<field name="requested_date" widget="date"/>

                                    <br/>
<strong>Assigned:</strong>
<field name="user_id"/>

                                </div>
                                <div t-if="record.priority.raw_value == 'high'">
                                    <span class="badge badge-danger">🔥 High Priority</span>
                                </div>
<div t-if="record.state.raw_value == 'submitted'">
    <span class="badge badge-warning">⏳ Awaiting Approval</span>
</div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

<!-- Enhanced Tree view for Portal Requests -->
    <record id="portal_request_view_tree" model="ir.ui.view">
        <field name="name">portal.request.view.tree</field>
        <field name="model">portal.request</field>
        <field name="arch" type="xml">
<tree decoration-muted="not active" decoration-info="state == 'draft'" decoration-warning="state == 'submitted'" decoration-success="state == 'approved'" decoration-danger="state == 'rejected'">
                <field name="name"/>
<field name="partner_id" optional="show"/>
<field name="request_type" optional="show"/>
<field name="priority" widget="priority" optional="show"/>
<field name="state" decoration-info="state == 'draft'" decoration-warning="state == 'submitted'" decoration-success="state == 'approved'"/>
<field name="requested_date" optional="show"/>
<field name="user_id" widget="many2one_avatar_user" optional="show"/>
<field name="active" invisible="1"/>
<field name="company_id" groups="base.group_multi_company" optional="hide"/>

            </tree>
        </field>
    </record>

<!-- Enhanced Form view for Portal Requests -->
    <record id="portal_request_view_form" model="ir.ui.view">
        <field name="name">portal.request.view.form</field>
        <field name="model">portal.request</field>
        <field name="arch" type="xml">
            <form>
                <header>
<button name="action_submit" string="Submit Request" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'draft')]}"/>
<button name="action_approve" string="Approve" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'submitted')]}" groups="records_management.group_records_manager"/>
<button name="action_reject" string="Reject" type="object" attrs="{'invisible': [('state', '!=', 'submitted')]}" groups="records_management.group_records_manager"/>
<button name="action_reset_to_draft" string="Reset to Draft" type="object" attrs="{'invisible': [('state', 'in', ['draft', 'approved'])]}"/>
<field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>

                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
<button class="oe_stat_button" type="action" icon="fa-tasks" attrs="{'invisible': [('related_tasks_count', '=', 0)]}" context="{'default_portal_request_id': id}">
    <div class="o_stat_info">
        <field name="related_tasks_count" widget="statinfo" string="Tasks"/>
    </div>
</button>
                        <button class="oe_stat_button" type="object" name="action_view_related_documents" icon="fa-file-text-o">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Documents</span>
                            </div>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="request_type"/>
                            <field name="partner_id"/>
<field name="requested_date"/>

                            <field name="priority"/>
                        </group>
                        <group>
<field name="user_id"/>

                            <field name="due_date"/>
                            <field name="estimated_hours"/>
                            <field name="actual_hours"/>
<field name="active"/>
<field name="company_id" groups="base.group_multi_company"/>

                        </group>
                    </group>

                    <notebook>
                        <page string="Request Details">
                            <field name="description" nolabel="1" placeholder="Request description and details..."/>
                            <group>
                                <field name="special_instructions" placeholder="Special handling instructions..."/>
                                <field name="customer_contact_info"/>
                            </group>
                        </page>
                        <page string="Processing Notes">
                            <group>
                                <field name="processing_notes" placeholder="Internal processing notes..."/>
                                <field name="completion_notes" placeholder="Completion summary and notes..."/>
                                <field name="customer_feedback" placeholder="Customer feedback on completed request..."/>
                            </group>
                        </page>
<page string="Signatures &amp; Approvals">
                            <group>
                                <field name="customer_signature" widget="signature"/>
                                <field name="staff_signature" widget="signature"/>
                                <field name="requires_approval"/>
                                <field name="approval_status" attrs="{'invisible': [('requires_approval', '=', False)]}"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

<!-- Enhanced Search view for Portal Requests -->
    <record id="portal_request_view_search" model="ir.ui.view">
        <field name="name">portal.request.view.search</field>
        <field name="model">portal.request</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="partner_id"/>
                <field name="request_type"/>
<field name="user_id"/>

<!-- State-based filters -->
<filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
<filter string="Submitted" name="submitted" domain="[('state', '=', 'submitted')]"/>
<filter string="Approved" name="approved" domain="[('state', '=', 'approved')]"/>
<filter string="Rejected" name="rejected" domain="[('state', '=', 'rejected')]"/>
                
<!-- Priority and date filters -->
                <filter string="High Priority" name="high_priority" domain="[('priority', '=', 'high')]"/>
<filter string="Overdue" name="overdue" domain="[('due_date', '&lt;', context_today())]"/>
<filter string="Active" name="active" domain="[('active', '=', True)]"/>


                <separator/>
<filter string="My Requests" name="my_requests" domain="[('user_id', '=', uid)]"/>


                <group expand="0" string="Group By">
<filter string="Customer" name="customer" context="{'group_by': 'partner_id'}"/>
<filter string="Request Type" name="request_type" context="{'group_by': 'request_type'}"/>
<filter string="Status" name="state" context="{'group_by': 'state'}"/>
<filter string="Priority" name="priority" context="{'group_by': 'priority'}"/>
<filter string="Assigned User" name="user" context="{'group_by': 'user_id'}"/>
<separator/>
<filter string="Request Date" name="request_date" context="{'group_by': 'requested_date:month'}"/>

                </group>
            </search>
        </field>
    </record>

<!-- Enhanced Action for Portal Requests -->
    <record id="action_portal_request" model="ir.actions.act_window">
        <field name="name">Portal Requests</field>
        <field name="res_model">portal.request</field>
        <field name="view_mode">kanban,tree,form</field>
<field name="domain">[]</field>
<field name="context">{
            'default_user_id': uid,
            'search_default_active': 1,
            'search_default_my_requests': 1
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
🌐 Create your first Portal Request!
            </p>
<p>
    <b>Enterprise Portal Request Management:</b>
    <br/>
                • 📝 Streamlined customer request processing<br/>
                • 🔄 Advanced workflow automation and approvals<br/>
                • 📊 Real-time status tracking and notifications<br/>
                • 🔗 Full integration with Records Management system<br/>
                • 👥 Collaborative request handling with activity tracking<br/>
                • 📈 Comprehensive analytics and reporting<br/>
                • ⚡ Automated notifications and escalations<br/>
                • 🔒 NAID compliance and audit trail management<br/>
                • 📱 Mobile-friendly portal access and management
</p>
        </field>
    </record>

<!-- Enhanced Tree view for Portal Requests -->
<record id="portal_request_view_tree" model="ir.ui.view">
    <field name="name">portal.request.view.tree</field>
    <field name="model">portal.request</field>
    <field name="arch" type="xml">
        <tree decoration-muted="not active" decoration-info="state == 'draft'" decoration-warning="state == 'submitted'" decoration-success="state == 'approved'" decoration-danger="state == 'rejected'">
            <field name="name"/>
            <field name="partner_id" optional="show"/>
            <field name="request_type" optional="show"/>
            <field name="priority" widget="priority" optional="show"/>
            <field name="state" decoration-info="state == 'draft'" decoration-warning="state == 'submitted'" decoration-success="state == 'approved'"/>
            <field name="requested_date" optional="show"/>
            <field name="user_id" widget="many2one_avatar_user" optional="show"/>
            <field name="active" invisible="1"/>
            <field name="company_id" groups="base.group_multi_company" optional="hide"/>
        </tree>
    </field>
</record>

<!-- Enhanced Form view for Portal Requests -->
<record id="portal_request_view_form" model="ir.ui.view">
    <field name="name">portal.request.view.form</field>
    <field name="model">portal.request</field>
    <field name="arch" type="xml">
        <form>
            <header>
                <button name="action_submit" string="Submit Request" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'draft')]}"/>
                <button name="action_approve" string="Approve" type="object" class="oe_highlight" attrs="{'invisible': [('state', '!=', 'submitted')]}" groups="records_management.group_records_manager"/>
                <button name="action_reject" string="Reject" type="object" attrs="{'invisible': [('state', '!=', 'submitted')]}" groups="records_management.group_records_manager"/>
                <button name="action_reset_to_draft" string="Reset to Draft" type="object" attrs="{'invisible': [('state', 'in', ['draft', 'approved'])]}"/>
                <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
            </header>
            <sheet>
                <div class="oe_button_box" name="button_box">
                    <button class="oe_stat_button" type="action" icon="fa-tasks" attrs="{'invisible': [('related_tasks_count', '=', 0)]}" context="{'default_portal_request_id': id}">
                        <div class="o_stat_info">
                            <field name="related_tasks_count" class="o_stat_value"/>
                            <span class="o_stat_text">Tasks</span>
                        </div>
                    </button>
                    <button class="oe_stat_button" type="action" icon="fa-comments" attrs="{'invisible': [('message_count', '=', 0)]}">
                        <div class="o_stat_info">
                            <field name="message_count" class="o_stat_value"/>
                            <span class="o_stat_text">Messages</span>
                        </div>
                    </button>
                </div>

                <div class="oe_title">
                    <label for="name"/>
                    <h1>
                        <field name="name" placeholder="Portal Request Title..."/>
                    </h1>
                </div>

                <group>
                    <group>
                        <field name="partner_id" options="{'no_create': True, 'no_create_edit': True}"/>
                        <field name="request_type"/>
                        <field name="priority" widget="priority"/>
                        <field name="requested_date"/>
                    </group>
                    <group>
                        <field name="user_id" options="{'no_create': True, 'no_create_edit': True}"/>
                        <field name="assigned_to" options="{'no_create': True, 'no_create_edit': True}" attrs="{'invisible': [('state', 'in', ['draft'])]}"/>
                        <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        <field name="active"/>
                    </group>
                </group>

                <notebook>
                    <page string="📋 Request Details">
                        <group>
                            <group>
                                <field name="description" placeholder="Detailed description of the request..."/>
                                <field name="special_instructions" placeholder="Special handling instructions..."/>
                            </group>
                            <group>
                                <field name="estimated_completion"/>
                                <field name="actual_completion" attrs="{'invisible': [('state', '!=', 'approved')]}"/>
                                <field name="urgency_level"/>
                            </group>
                        </group>
                    </page>
                    <page string="📎 Attachments &amp; Documents">
                        <field name="attachment_ids" widget="many2many_binary"/>
                    </page>
                    <page string="🔒 Compliance &amp; Audit">
                        <group>
                            <group string="Tracking Information">
                                <field name="create_date" readonly="1"/>
                                <field name="write_date" readonly="1"/>
                                <field name="create_uid" readonly="1"/>
                                <field name="write_uid" readonly="1"/>
                            </group>
                            <group string="NAID Compliance">
                                <field name="naid_audit_required"/>
                                <field name="chain_of_custody_id" attrs="{'invisible': [('naid_audit_required', '=', False)]}"/>
                            </group>
                        </group>
                    </page>
                </notebook>
            </sheet>
            <!-- Mail Thread Integration -->
            <div class="oe_chatter">
                <field name="message_follower_ids"/>
                <field name="activity_ids"/>
                <field name="message_ids"/>
            </div>
        </form>
    </field>
</record>

<!-- Enhanced Kanban view -->
<record id="portal_request_view_kanban" model="ir.ui.view">
    <field name="name">portal.request.view.kanban</field>
    <field name="model">portal.request</field>
    <field name="arch" type="xml">
        <kanban default_group_by="state" class="o_kanban_small_column o_kanban_portal_request">
            <field name="priority"/>
            <field name="partner_id"/>
            <field name="user_id"/>
            <field name="requested_date"/>
            <field name="state"/>
            <field name="activity_ids"/>
            <field name="activity_state"/>
            <templates>
                <t t-name="kanban-box">
                    <div t-attf-class="oe_kanban_card oe_kanban_global_click #{kanban_color(record.priority.raw_value)}">
                        <div class="oe_kanban_content">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                    </strong>
                                </div>
                                <div class="o_kanban_manage_button_section">
                                    <a class="o_kanban_manage_toggle_button" href="#">
                                        <i class="fa fa-ellipsis-v" role="img" aria-label="Manage" title="Manage"/>
                                    </a>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div class="o_kanban_record_subtitle">
                                    <field name="partner_id"/>
                                </div>
                                <div class="o_kanban_record_subtitle">
                                    <i class="fa fa-calendar" role="img" aria-label="Date" title="Date"/>
                                    <field name="requested_date"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <div class="o_priority_star">
                                        <field name="priority" widget="priority" readonly="1"/>
                                    </div>
                                    <t t-if="record.activity_ids.raw_value.length > 0">
                                        <div class="o_kanban_inline_block">
                                            <field name="activity_ids" widget="kanban_activity"/>
                                        </div>
                                    </t>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <img t-att-src="kanban_image('res.users', 'avatar_128', record.user_id.raw_value)" t-att-title="record.user_id.value" t-att-alt="record.user_id.value" class="oe_kanban_avatar"/>
                                </div>
                            </div>
                        </div>
                        <div class="oe_kanban_manage_button_section">
                            <div class="o_kanban_manage_view">
                                <a type="edit" class="btn btn-primary btn-sm">Edit</a>
                                <a type="delete" class="btn btn-secondary btn-sm">Delete</a>
                            </div>
                        </div>
                    </div>
                </t>
            </templates>
        </kanban>
    </field>
</record>

<!-- Enhanced Search view -->
<record id="portal_request_view_search" model="ir.ui.view">
    <field name="name">portal.request.view.search</field>
    <field name="model">portal.request</field>
    <field name="arch" type="xml">
        <search>
            <field name="name" string="Request Name" filter_domain="[('name', 'ilike', self)]"/>
            <field name="partner_id" string="Customer"/>
            <field name="user_id" string="Assigned User"/>
            <field name="request_type"/>
            <separator/>
            <filter string="Active" name="active" domain="[('active', '=', True)]"/>
            <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
            <filter string="Submitted" name="submitted" domain="[('state', '=', 'submitted')]"/>
            <filter string="Approved" name="approved" domain="[('state', '=', 'approved')]"/>
            <separator/>
            <filter string="High Priority" name="high_priority" domain="[('priority', '=', '3')]"/>
            <filter string="My Requests" name="my_requests" domain="[('user_id', '=', uid)]"/>
            <separator/>
            <filter string="This Week" name="week" domain="[('requested_date', '&gt;=', (context_today() - relativedelta(weeks=1)).strftime('%Y-%m-%d'))]"/>
            <filter string="This Month" name="month" domain="[('requested_date', '&gt;=', (context_today() - relativedelta(months=1)).strftime('%Y-%m-%d'))]"/>
            <separator/>
            <filter invisible="1" string="Late Activities" name="activities_overdue" domain="[('activity_ids.date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]" help="Show requests with overdue activities"/>
            <filter invisible="1" string="Today Activities" name="activities_today" domain="[('activity_ids.date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
            <separator/>
            <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            <group expand="0" string="Group By">
                <filter string="Customer" name="customer" context="{'group_by': 'partner_id'}"/>
                <filter string="Request Type" name="request_type" context="{'group_by': 'request_type'}"/>
                <filter string="Status" name="state" context="{'group_by': 'state'}"/>
                <filter string="Priority" name="priority" context="{'group_by': 'priority'}"/>
                <filter string="Assigned User" name="user" context="{'group_by': 'user_id'}"/>
                <separator/>
                <filter string="Request Date" name="request_date" context="{'group_by': 'requested_date:month'}"/>
            </group>
        </search>
    </field>
</record>

<!-- Enhanced Action -->
<record id="portal_request_action" model="ir.actions.act_window">
    <field name="name">Portal Requests</field>
    <field name="type">ir.actions.act_window</field>
    <field name="res_model">portal.request</field>
    <field name="view_mode">kanban,tree,form</field>
    <field name="domain">[]</field>
    <field name="context">{
            'default_user_id': uid,
            'search_default_active': 1,
            'search_default_my_requests': 1
        }</field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
                🌐 Create your first Portal Request!
        </p>
        <p>
            <b>Enterprise Portal Request Management:</b>
            <br/>
                • 📝 Streamlined customer request processing<br/>
                • 🔄 Advanced workflow automation and approvals<br/>
                • 📊 Real-time status tracking and notifications<br/>
                • 🔗 Full integration with Records Management system<br/>
                • 👥 Collaborative request handling with activity tracking<br/>
                • 📈 Comprehensive analytics and reporting<br/>
                • ⚡ Automated notifications and escalations<br/>
                • 🔒 NAID compliance and audit trail management<br/>
                • 📱 Mobile-friendly portal access and management
</p>
</field>
</record>

<!-- Enhanced Menu Structure -->
    <menuitem id="portal_request_menu" 
              name="Portal Requests" 
              parent="menu_records_management_root" 
              action="portal_request_action" 
              sequence="10"
              groups="records_management.group_records_manager"/>

<!-- Additional Menu Items for Better Navigation -->
<menuitem id="portal_request_draft_menu" name="Draft Requests" parent="portal_request_menu" action="portal_request_action" sequence="1" groups="records_management.group_records_user"/>

<menuitem id="portal_request_submitted_menu" name="Submitted Requests" parent="portal_request_menu" action="portal_request_action" sequence="2" groups="records_management.group_records_manager"/>

</odoo>
