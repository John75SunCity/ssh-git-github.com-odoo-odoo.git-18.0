<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for base.rate -->
    <record id="view_base_rate_tree" model="ir.ui.view">
        <field name="name">base.rate.tree</field>
        <field name="model">base.rate</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for base.rate -->
    <record id="view_base_rate_form" model="ir.ui.view">
        <field name="name">base.rate.form</field>
        <field name="model">base.rate</field>
        <field name="arch" type="xml">
            <form string="Base Rate">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for base.rate -->
    <record id="view_base_rate_search" model="ir.ui.view">
        <field name="name">base.rate.search</field>
        <field name="model">base.rate</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action base.rate -->
    <record id="action_base_rate" model="ir.actions.act_window">
        <field name="name">Base Rate</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">base.rate</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Base Rate.
            </p>
        </field>
    </record>

</odoo>
