<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Minimal action - let Odoo auto-generate views for pos.config -->
    <record id="action_pos_config_records" model="ir.actions.act_window">
        <field name="name">POS Configuration</field>
        <field name="res_model">pos.config</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first record
            </p>
            <p>
                Configure point-of-sale integration for walk-in services.
            </p>
        </field>
    </record>
</odoo>
