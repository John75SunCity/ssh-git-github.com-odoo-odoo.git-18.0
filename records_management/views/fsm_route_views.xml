<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Tree View -->
        <record id="fsm_route_view_tree" model="ir.ui.view">
            <field name="name">fsm.route.view.tree</field>
            <field name="model">fsm.route</field>
            <field name="arch" type="xml">
                <tree string="FSM Route">
                    <!-- Add fields here -->
                </tree>
            </field>
        </record>

        <!-- Form View -->
        <record id="fsm_route_view_form" model="ir.ui.view">
            <field name="name">fsm.route.view.form</field>
            <field name="model">fsm.route</field>
            <field name="arch" type="xml">
                <form string="FSM Route">
                    <sheet>
                        <group>
                            <!-- Add fields here -->
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Search View -->
        <record id="fsm_route_view_search" model="ir.ui.view">
            <field name="name">fsm.route.view.search</field>
            <field name="model">fsm.route</field>
            <field name="arch" type="xml">
                <search string="Search FSM Route">
                    <!-- Add search fields here -->
                </search>
            </field>
        </record>

        <!-- Action -->
        <record id="action_fsm_route" model="ir.actions.act_window">
            <field name="name">FSM Route</field>
            <field name="res_model">fsm.route</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- Menu -->
        <!--
        <menuitem
            id="menu_fsm_route"
            name="FSM Route"
            action="action_fsm_route"
            parent="REPLACE_WITH_PARENT_MENU_ID"
            sequence="10"/>
        -->

    </data>
</odoo>
