<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for proj.task.ext -->
    <record id="view_proj_task_ext_tree" model="ir.ui.view">
        <field name="name">proj.task.ext.tree</field>
        <field name="model">proj.task.ext</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for proj.task.ext -->
    <record id="view_proj_task_ext_form" model="ir.ui.view">
        <field name="name">proj.task.ext.form</field>
        <field name="model">proj.task.ext</field>
        <field name="arch" type="xml">
            <form string="Proj Task Ext">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for proj.task.ext -->
    <record id="view_proj_task_ext_search" model="ir.ui.view">
        <field name="name">proj.task.ext.search</field>
        <field name="model">proj.task.ext</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action proj.task.ext -->
    <record id="action_proj_task_ext" model="ir.actions.act_window">
        <field name="name">Proj Task Ext</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">proj.task.ext</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Proj Task Ext.
            </p>
        </field>
    </record>

</odoo>
