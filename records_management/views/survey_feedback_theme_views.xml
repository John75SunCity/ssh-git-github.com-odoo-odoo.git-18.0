<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View survey.feedback.theme View List -->
    <record id="view_survey_feedback_theme_list" model="ir.ui.view">
        <field name="name">view.survey.feedback.theme.list</field>
        <field name="model">survey.feedback.theme</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View survey.feedback.theme form -->
    <record id="view_survey_feedback_theme_form" model="ir.ui.view">
        <field name="name">view.survey.feedback.theme.form</field>
        <field name="model">survey.feedback.theme</field>
        <field name="arch" type="xml">
            <form string="Survey Feedback Theme">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View survey.feedback.theme search -->
    <record id="view_survey_feedback_theme_search" model="ir.ui.view">
        <field name="name">view.survey.feedback.theme.search</field>
        <field name="model">survey.feedback.theme</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action survey.feedback.theme -->
    <record id="action_survey_feedback_theme" model="ir.actions.act_window">
        <field name="name">Survey Feedback Theme</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">survey.feedback.theme</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Survey Feedback Theme.
            </p>
        </field>
    </record>

</odoo>
