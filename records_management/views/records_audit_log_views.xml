<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.audit.log -->
    <record id="view_records_audit_log_tree" model="ir.ui.view">
        <field name="name">records.audit.log.tree</field>
        <field name="model">records.audit.log</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.audit.log -->
    <record id="view_records_audit_log_form" model="ir.ui.view">
        <field name="name">records.audit.log.form</field>
        <field name="model">records.audit.log</field>
        <field name="arch" type="xml">
            <form string="Records Audit Log">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.audit.log -->
    <record id="view_records_audit_log_search" model="ir.ui.view">
        <field name="name">records.audit.log.search</field>
        <field name="model">records.audit.log</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.audit.log -->
    <record id="action_records_audit_log" model="ir.actions.act_window">
        <field name="name">Records Audit Log</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.audit.log</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Audit Log.
            </p>
        </field>
    </record>

</odoo>
