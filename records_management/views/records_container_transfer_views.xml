<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree view for records.container.transfer -->
    <record id="view_records_container_transfer_tree" model="ir.ui.view">
        <field name="name">records.container.transfer.tree</field>
        <field name="model">records.container.transfer</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Form view for records.container.transfer -->
    <record id="view_records_container_transfer_form" model="ir.ui.view">
        <field name="name">records.container.transfer.form</field>
        <field name="model">records.container.transfer</field>
        <field name="arch" type="xml">
            <form string="Records Container Transfer">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view for records.container.transfer -->
    <record id="view_records_container_transfer_search" model="ir.ui.view">
        <field name="name">records.container.transfer.search</field>
        <field name="model">records.container.transfer</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.container.transfer -->
    <record id="action_records_container_transfer" model="ir.actions.act_window">
        <field name="name">Records Container Transfer</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.container.transfer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Container Transfer.
            </p>
        </field>
    </record>

</odoo>
