<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View records.billing.contact View List -->
    <record id="view_records_billing_contact_list" model="ir.ui.view">
        <field name="name">view.records.billing.contact.list</field>
        <field name="model">records.billing.contact</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
            </list>
        </field>
    </record>

    <!-- View records.billing.contact form -->
    <record id="view_records_billing_contact_form" model="ir.ui.view">
        <field name="name">view.records.billing.contact.form</field>
        <field name="model">records.billing.contact</field>
        <field name="arch" type="xml">
            <form string="Records Billing Contact">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Name..."/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- View records.billing.contact search -->
    <record id="view_records_billing_contact_search" model="ir.ui.view">
        <field name="name">view.records.billing.contact.search</field>
        <field name="model">records.billing.contact</field>
        <field name="arch" type="xml">
            <search>
                <group expand="1" string="Group By">
                    <filter string="Name" name="name" domain="[]" context="{'group_by':'name'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action records.billing.contact -->
    <record id="action_records_billing_contact" model="ir.actions.act_window">
        <field name="name">Records Billing Contact</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">records.billing.contact</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Records Billing Contact.
            </p>
        </field>
    </record>

</odoo>
